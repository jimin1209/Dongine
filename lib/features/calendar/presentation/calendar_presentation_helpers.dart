part of 'calendar_screen.dart';

IconData _eventTypeIcon(String type) => eventTypeIcon(type);

String _eventTypeLabel(String type) => eventTypeLabel(type);

String _eventTypeColor(String type) => eventTypeColor(type);

Color _parseColor(String hex) => parseHexColor(hex);
