# 시제품 데모 워크스루 (3–5분)

> 이 문서는 **동이네(Dongine)** 시제품을 처음 보는 청중 앞에서 3–5분 안에 핵심 기능을 보여주기 위한 시연 시나리오다.
> 시연자는 이 문서를 위에서 아래로 따라가면 된다.

---

## 앱이 실제로 열리는 순서 (코드 기준)

시연을 망치지 않으려면 아래 **라우팅**을 알고 있으면 된다 (`GoRouter`, `initialLocation: /splash`).

| 상태 | 다음 화면 |
|------|----------|
| 앱 실행 | **스플래시** (`/splash`) — Auth·가족 정보 로딩 |
| 로그인 안 됨 | **온보딩** (`/onboarding`) → 버튼으로 **로그인** (`/login`) |
| 로그인 됨, 가족 없음 | **가족 그룹 설정** (`/family-setup`) — 가족 만들기·초대 코드 참가 |
| 로그인 됨, 가족 있음 | **홈** (`/home`) — 하단 탭: 홈·채팅·**지도**·파일·캘린더 |

- 온보딩은 **첫 실행·로그아웃 후** 등 계정이 없을 때만 거친다. 사전에 만든 계정으로 로그인하면 스플래시 직후 홈 또는 가족 그룹 설정으로 간다.
- **설정**(데모 시드/초기화): 홈 우상단 톱니바퀴 → 앱바 제목 **「설정」** (`/settings`).

---

## 권장 시연 순서 (한눈에)

1. **환경·Firebase·Functions** 준비 및 `tool/preflight.sh` (아래 0절).
2. **데모 직전 Smoke**(강력 권장): [demo-smoke-push-map-calendar.md](./demo-smoke-push-map-calendar.md)로 푸시·**지도** 탭·(선택) Google Calendar를 1~2분 확인 — 시연 중 첫 실패를 줄인다.
3. **Debug 빌드**로 앱 실행 (`flutter run`). Release 빌드에는 데모 시드 버튼이 없다.
4. **로그인 → 가족** (또는 기존 계정·가족)까지 끝낸 뒤, 설정에서 **데모 데이터 채우기** 또는 **초기화 → 채우기** (3절).
5. **채팅 → 할 일 → 장보기 → 가계부 → 캘린더**(앱 일정·플래너) 순으로 화면을 밀어 넣는다.
6. **지도** 탭에서 위치 공유·마커를 짧게 확인(네이버맵·권한).
7. **푸시**: 기기 B를 백그라운드에 두고 기기 A에서 트리거 → 알림 탭·딥링크.
8. 남는 시간에 홈·파일·앨범 등 선택 시연.

---

## 0. 데모 전 준비 (사람이 직접 해야 하는 항목)

아래 항목이 **모두** 완료되어 있어야 시연이 원활하다. 상세 절차는 [manual-build-inputs.md](./manual-build-inputs.md)와 [release-checklist.md](./release-checklist.md)를 참고한다.

### 외부 서비스 설정

| # | 항목 | 확인 방법 |
|---|------|----------|
| 1 | **Firebase 프로젝트** 생성 및 Blaze 요금제 전환 | Firebase Console에서 `dongine-13214` 프로젝트 확인 |
| 2 | **Firebase Auth**에서 이메일/비밀번호 로그인 활성화 | Firebase Console → Authentication → Sign-in method |
| 3 | **Firebase 설정 파일** 3종 배치 | `android/app/google-services.json`, `ios/Runner/GoogleService-Info.plist`, `lib/firebase_options.dart` |
| 4 | **Firestore 규칙·인덱스** 배포 | `firebase deploy --only firestore:rules,firestore:indexes --project=dongine-13214` |
| 5 | **Cloud Functions** 배포 (푸시 알림용) | `firebase deploy --only functions --project=dongine-13214` |
| 6 | **Storage 규칙** 배포 | `firebase deploy --only storage --project=dongine-13214` |
| 7 | **네이버맵 Client ID** 발급 및 입력 | `android/gradle.properties`, `ios/Flutter/Debug.xcconfig` 에 `NAVER_MAP_CLIENT_ID` 값 |
| 8 | **Android 푸시**: FCM 자동 설정 (google-services.json 포함 시 완료) | — |
| 9 | **iOS 푸시**: APNs 키 발급 → Firebase Console 등록 | Firebase Console → Cloud Messaging → APNs |

### 기기 · 빌드

| # | 항목 | 확인 방법 |
|---|------|----------|
| 1 | 시연용 기기(또는 에뮬레이터) **2대** 준비 — 가족 참가 흐름을 보여주려면 2대가 필요 | — |
| 2 | 프리플라이트 스크립트 통과 | `bash tool/preflight.sh` → 실패 0건 |
| 3 | `flutter pub get && flutter analyze` 경고 없음 | — |
| 4 | 앱 빌드·설치 완료 | `flutter run` 또는 사전 빌드된 APK/IPA 설치 |

### 시연 직전

| # | 항목 | 비고 |
|---|------|------|
| 1 | 기기 인터넷 연결(Wi-Fi) 확인 | Firestore 실시간 동기화에 필요 |
| 2 | 테스트 계정 2개 사전 가입 — 또는 시연 중 즉석 가입 | 예: `demo1@test.com` / `demo2@test.com` |
| 3 | 데모 샘플 데이터 정리 (선택) | **Debug**에서 홈 → **설정** → **데모 데이터 초기화** — 제목이 `[DEMO]`로 시작하는 할 일·장보기·가계부·일정만 삭제되고, 그 외 가족 데이터는 유지된다. 완전히 새 가족이 필요하면 Firestore/콘솔에서 별도 정리 |
| 4 | 화면 미러링 설정 (청중이 볼 수 있도록) | scrcpy, QuickTime 미러링, 프로젝터 등 |

---

## 1단계: 첫 실행 — 스플래시·온보딩·로그인 *(~30초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 1-1 | 앱 실행 → **스플래시**에서 로그인·가족 정보 확인 | 짧은 로딩 메시지 후 자동 전환 |
| 1-2 | (미로그인 시) **온보딩** — 기능 소개 후 「새 계정으로 시작하기」 또는 「로그인」 | 앱 이름 **동이네**, 가족 허브 컨셉 |
| 1-3 | **회원가입/로그인** (이메일 + 비밀번호) | Firebase Auth 이메일·비밀번호 |
| 1-4 | 로그인 완료 후 **가족이 없으면** **가족 그룹 설정** 화면으로 이동 | 가족이 이미 있으면 **홈**으로 바로 이동 |

**시연 팁**: 시간 절약을 위해 사전 가입한 계정으로 로그인하면 온보딩을 건너뛴다.

---

## 2단계: 가족 생성 & 참가 *(~40초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 2-1 | **기기 A**에서 **가족 그룹 설정** 화면 → **새 가족 만들기** → 가족 이름 입력 → 생성 | 생성 즉시 완료, **6자리** 초대 코드 자동 발급 |
| 2-2 | 초대 코드 확인 — 이후 **홈 → 설정(톱니바퀴)** 에서 가족·초대 코드 관리 | **7일** 만료 정책 언급 (`AppConstants`) |
| 2-3 | **기기 B**에서 로그인 → 같은 화면에서 **초대 코드로 참가** → 코드 입력 | 참가 즉시 같은 가족 구성원으로 등록 |
| 2-4 | 양쪽 기기에서 **홈** 대시보드 진입 확인 | Firestore 실시간 동기화 |

**시연 팁**: 기기가 1대뿐이면 2-3·2-4는 구두로 설명하고 넘어간다.

---

## 3단계: 데모 데이터 시드·초기화 *(~10–30초)*

> **Debug 빌드에서만** 노출된다 (`kDebugMode`). 화면: **홈 → 우상단 설정 → 스크롤 하단**.

| 버튼 | 동작 |
|------|------|
| **데모 데이터 채우기** | 현재 가족에 할 일·장보기·가계부·캘린더 샘플을 한 번에 추가. 이미 데모가 있으면 스낵바 **「이미 데모 데이터가 존재합니다.」** — 이 경우 **데모 데이터 초기화** 후 다시 누른다. |
| **데모 데이터 초기화** | 제목/이름이 **`[DEMO]`** 로 시작하는 문서만 삭제한다. 사용자가 만든 데이터는 건드리지 않는다. 삭제 건수는 스낵바로 요약된다. |

채워지는 데이터(모든 항목 이름에 `[DEMO]` 접두사가 붙음):
- 할 일 **4건** (우유 사오기, 거실 청소, 병원 예약 확인, 수학 문제집 풀기)
- 장보기 **5건** (바나나, 두부, 삼겹살 600g, 우유 1L, 주방세제)
- 가계부 **5건** (마트 장보기, 지하철 교통카드 충전, 세탁소, 소아과 진료, 문제집 구매)
- 일정 **4건** (가족 저녁 식사, 결혼기념일, 소아과 방문, 주말 나들이 — 오늘 기준 상대 날짜로 생성)

이렇게 하면 빈 화면 없이 각 탭 UI를 바로 보여줄 수 있다.

---

## 4단계: 채팅 *(~40초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 4-1 | 하단 탭에서 **채팅** 진입 | 실시간 메시지 목록 |
| 4-2 | 메시지 전송 → 상대 기기에서 수신 확인 | Firestore 실시간 동기화, 읽음 처리 표시 |
| 4-3 | **슬래시 커맨드** 시연: `/todo 빨래 개기` 입력 | 채팅에서 바로 할 일이 생성되는 것을 보여줌 |
| 4-4 | (선택) `/expense 점심 12000` 또는 `/cart 식빵` | 채팅 한 줄로 가계부·장보기 연동 |

**핵심 메시지**: "채팅 하나로 할 일, 가계부, 장보기까지 바로 연결된다."

---

## 5단계: 할 일 *(~30초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 5-1 | 하단 탭에서 **할 일** 진입 | 목록 UI, 시드 데이터 또는 4-3에서 만든 항목 표시 |
| 5-2 | 항목 하나 탭하여 **완료 토글** | 체크 표시 즉시 반영, 상대 기기에도 동기화 |
| 5-3 | **+ 버튼**으로 새 할 일 추가 (담당자 지정, 마감일 설정) | 가족 구성원에게 직접 할당 가능 |

---

## 6단계: 장보기 *(~30초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 6-1 | **장보기** 탭 진입 | 품목 목록 + 수량 표시 |
| 6-2 | 품목 추가 시 **추천 목록** 노출 | 과거 구매 빈도 기반 상위 10개 추천 |
| 6-3 | 이미 있는 품목과 같은 이름 추가 → **수량 자동 병합** | 중복 방지 로직 시연 |
| 6-4 | 품목 체크(구매 완료) | 실시간 동기화 |

---

## 7단계: 가계부 *(~30초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 7-1 | **가계부** 탭 진입 | 월별 지출 합계, 카테고리별 차트 |
| 7-2 | 지출 내역 스크롤 | 시드 데이터 또는 `/expense`로 만든 항목 확인 |
| 7-3 | 새 지출 추가 (금액, 카테고리, 메모) | 추가 즉시 합계·차트 갱신 |

---

## 8단계: 캘린더 *(~30초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 8-1 | 하단 **캘린더** 탭 진입 | 월간 뷰(TableCalendar), 일정 있는 날짜 표시 |
| 8-2 | 시드된 `[DEMO]` 일정이 보이는 날짜 탭 → 상세 | 유형 필터(식사·나들이·기념일·병원 등) |
| 8-3 | 새 일정 추가 (시간, 유형 선택) | CRUD, Cloud Functions 일정 생성 알림(조건 충족 시) |
| 8-4 | (선택) **플래너** 탭 전환 | 다가오는 일정 리스트 UI |

**Google Calendar**: 앱 안에서 가져오기/보내기는 **OAuth·Calendar API** 설정이 필요하다. 시연 직전에 동작만 확인하려면 [demo-smoke-push-map-calendar.md](./demo-smoke-push-map-calendar.md) 3절을 따른다. 본 8단계에서는 **앱 일정·시드 데이터**만으로도 충분히 시연 가능하다.

---

## 9단계: 지도(위치 공유) *(~20초)*

하단 네비게이션 라벨은 **「지도」** (`/map`, 위치 공유 화면).

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 9-1 | **지도** 탭 진입 | 상단 **위치 서비스·권한** 상태 배너 |
| 9-2 | **위치 공유** 토글 ON | 네이버맵에 내 마커, Geolocator 스트림 + Firestore 업로드(설정 충족 시) |
| 9-3 | (기기 2대) 상대도 토글 ON 시 가족 마커 | 같은 가족만 표시 |

자세한 점검·실패 시 확인표는 [demo-smoke-push-map-calendar.md](./demo-smoke-push-map-calendar.md) 2절과 동일하다.

---

## 10단계: 푸시 알림 *(~20초)*

| 순서 | 동작 | 보여줄 포인트 |
|------|------|--------------|
| 10-1 | **기기 B**를 홈 화면 또는 다른 탭으로 보낸 뒤, **기기 A**에서 채팅 전송 또는 할 일·일정 등 생성 | Functions `onDocumentCreated` 트리거 |
| 10-2 | **기기 B**에서 푸시 또는 포그라운드 스낵바 수신 | FCM |
| 10-3 | 알림 탭 → `data.route` 기준 딥링크 | 채팅 `/chat`, 할 일 `/todo`, 장보기 `/cart`, 가계부 `/expense`, 일정 `/calendar` 등 |

**시연 팁**: 기기 1대면 Smoke 문서와 같이 구두로 보강. 자세한 점검은 [demo-smoke-push-map-calendar.md](./demo-smoke-push-map-calendar.md) 1절.

---

## 11단계: 마무리 & 추가 시연 *(선택, 잔여 시간 활용)*

시간이 남으면 아래 기능을 추가로 보여준다.

| 기능 | 시연 포인트 |
|------|------------|
| **홈 대시보드** | 할 일·장보기·가계부·일정 요약 카드 |
| **파일함** | 파일 업로드 → 폴더 생성 → 검색 |
| **앨범** | 앨범 생성 → 사진 업로드 → 커버 자동 설정 |
| **가족 전환** | 복수 가족 소속 시 설정에서 전환 |

---

## 시연 타임라인 요약

```
[0:00]  1단계  스플래시·온보딩·로그인   ~30초
[0:30]  2단계  가족 생성·참가          ~40초
[1:10]  3단계  데모 데이터 시드·초기화   ~10–30초
[1:40]  4단계  채팅 + 슬래시 커맨드     ~40초
[2:20]  5단계  할 일                   ~30초
[2:50]  6단계  장보기                  ~30초
[3:20]  7단계  가계부                  ~30초
[3:50]  8단계  캘린더                  ~30초
[4:20]  9단계  지도(위치 공유)         ~20초
[4:40]  10단계 푸시 알림              ~20초
[5:00]  11단계 마무리·추가 시연        잔여 시간
```

핵심 탭 시연만 합치면 **약 5분** — Smoke·시드·질의응답은 별도 버퍼를 잡는 것이 안전하다.

---

## 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| 로그인 실패 | Firebase Auth에 이메일 로그인 비활성화 | Firebase Console → Authentication → Sign-in method에서 활성화 |
| 데이터가 동기화되지 않음 | Firestore 규칙 미배포 또는 인덱스 누락 | `firebase deploy --only firestore` 재실행 |
| 푸시 알림 안 옴 | Cloud Functions 미배포 또는 FCM 토큰 미등록 | `firebase deploy --only functions` 후 앱 재시작 |
| 지도 빈 화면 | 네이버맵 Client ID 미설정 | `gradle.properties`, `xcconfig` 확인 |
| 데모 시드 버튼 안 보임 | Release 빌드 | Debug 빌드(`flutter run`)로 실행 |
| 「이미 데모 데이터가 존재합니다」 | 동일 가족에 이전 시드 잔존 | **데모 데이터 초기화** 후 다시 채우기 |
| 장보기 쿼리 오류 | Firestore 복합 인덱스 미배포 | `firebase deploy --only firestore:indexes` |
